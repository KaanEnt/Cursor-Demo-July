import { PageShell, mountPage } from './PageShell';

mountPage(
  <PageShell>
    <section className="container mx-auto max-w-6xl px-4 py-24">
      <h1 className="font-display text-4xl font-bold text-foreground">Pipeline Check</h1>
      <p className="mt-4 text-muted">Replace this with real content.</p>
    </section>
  </PageShell>,
);
